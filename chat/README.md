# chat API & test harness
* build the docker container Dockerfilechat
* run the docker container
* go to localhost and web form should appear with buttons 
* enter text and press submit to try it out
